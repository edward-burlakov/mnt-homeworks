---
dependency:
  name: galaxy
  enabled: false
driver:
  name: docker
lint: |
  ansible-lint .
  yamllint .
platforms:
  - name: Сentos8
    image: pycontribs/centos:8
    pre_build_image: true
  - name: Ubuntu
    image: pycontribs/ubuntu:latest
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
